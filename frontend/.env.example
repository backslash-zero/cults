# Sanity project connection — must match studio-cults/sanity.config.ts
SANITY_PROJECT_ID=dm4p8gdv
SANITY_DATASET=production
SANITY_API_VERSION=2026-01-01

# Read-only token. Create at manage.sanity.io -> project -> API -> Tokens
# ("Viewer" permission is enough). Leave blank if the dataset is public.
SANITY_API_READ_TOKEN=
